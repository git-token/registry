import Promise from 'bluebird'

export default function createRelayContract({
  _organization,
  _symbol,
  _registeredBy,
  _token,
  _date
}) {
  return new Promise((resolve, reject) => {
    
  })
}
